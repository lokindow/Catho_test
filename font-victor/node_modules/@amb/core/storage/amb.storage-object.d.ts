/**
 * @Serviço @name AMBStorageObject
 * Objeto de sessão do AMB 2.0.
 *
 * @description
 * Esta classe representa um objeto de sessão da aplicação. Para persistir
 * valores na sessão da aplicação utilizando a sessão do AMB, é necessário
 * sempre criar este tipo de objeto. Esta classe existe para padronizar o uso
 * da sessão.
 *
 * Para criar um objeto de sessão é necessário SEMPRE informar uma chave para o
 * objeto na sessão, caso a chave não seja informada será lançada uma exceção.
 *
 */
export declare class AMBStorageObject {
    /**
     * Chave de identificação do objeto na sessão, é obrigatório informar a chave.
     */
    private _key;
    /**
     * Valor a ser armazenado na sessão.
     */
    private _value;
    /**
     * Obtém a chave do o objeto de sessão.
     *
     * @return key Chave do objeto.
     */
    /**
     * Atribui uma chave para o objeto de sessão.
     *
     * @param key Chave do objeto.
     */
    key: string;
    /**
     * Obtém o valor do o objeto de sessão.
     *
     * @return value Valor do objeto.
     */
    /**
     * Atribui um valor para o objeto de sessão.
     *
     * @param value Valor do objeto.
     */
    value: string;
}
