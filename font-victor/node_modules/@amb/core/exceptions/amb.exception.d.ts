import { AMBSeveridade } from "./amb.enum.severidade";
/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
/**
 * @Exceção @name AMBException
 * Representa o componente padrão de erros da aplicação.
 *
 * @description
 * Esta classe define uma exceção de aplicação. Ao ser capturada pelo handler será
 * apresentado um modal mostrando a mensagem informada em sua criação.
 *
 * As aplicações poderão implementar suas próprias exceções customizadas, para isso devem
 * seguir este mesmo padrão de implementação.
 *
 * @example
 * class CustomException implements AMBException {
 * ...
 * }
 *
 * @implements Error
 *
 * @see {@link https://www.typescriptlang.org/docs/home.html}
 */
export declare class AMBException implements Error {
    /** Nome da exceção. */
    name: string;
    /** Mensagem de erro. */
    message: string;
    /** Severidade do erro */
    severidade: AMBSeveridade;
    /** Stacktrace do erro. */
    stack?: string;
    /**
     * @constructor
     * Construtor padrão.
     *
     * @param message Mensagem de erro.
     */
    constructor(message?: string, severidade?: AMBSeveridade);
}
