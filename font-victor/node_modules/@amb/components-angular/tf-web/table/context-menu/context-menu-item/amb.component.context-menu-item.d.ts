import { ElementRef, EventEmitter } from "@angular/core";
export declare class AMBContextItemComponent {
    icon: string;
    action: EventEmitter<Object>;
    targetData: Object;
    constructor(elementRef: ElementRef);
    clicked(): void;
}
