/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { ChangeDetectorRef } from '@angular/core';
import { CdkColumnDef } from '@angular/cdk/table';
import { AMBSort, AMBSortable } from './amb.sort';
import { AMBSortHeaderIntl } from './amb.sort-header-intl';
import { CanDisable } from './amb.disabled';
export declare class AMBSortHeaderBase {
}
export declare const _AMBSortHeaderMixinBase: (new (...args: any[]) => CanDisable) & typeof AMBSortHeaderBase;
/**
 * Aplica comportamento de classificação (clique para alterar o tipo) e estilos para um elemento, incluindo um
 * seta para exibir a direção de classificação atual.
 *
 * Deve ser fornecido com um ID e contido em uma diretiva AMBSort principal.
 *
 * Se usado em células de cabeçalho em um CdkTable, ele irá automaticamente usar o padrão de sua identificação de seu conteúdo
 * definição da coluna.
*/
export declare class AMBSortHeader extends _AMBSortHeaderMixinBase implements AMBSortable, CanDisable {
    _intl: AMBSortHeaderIntl;
    _sort: AMBSort;
    _cdkColumnDef: CdkColumnDef;
    private _rerenderSubscription;
    /**
     * ID deste cabeçalho de classificação. Se usado no contexto de um CdkColumnDef, isso será padrão para
     * o nome da coluna.
     */
    id: string;
    /** Define a posição da seta que é exibida quando ordenada. */
    arrowPosition: 'before' | 'after';
    /** Substitui o valor de inicialização do AMBSort que contém para este AMBSortable. */
    start: 'asc' | 'desc';
    /** Substitui o valor desativar do AMBSort que contém para este AMBSortable. */
    disableClear: boolean;
    private _disableClear;
    constructor(_intl: AMBSortHeaderIntl, changeDetectorRef: ChangeDetectorRef, _sort: AMBSort, _cdkColumnDef: CdkColumnDef);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    /** Manipula os eventos de clique no cabeçalho. */
    _handleClick(): void;
    /** Se este AMBSortHeader está atualmente classificado em ordem ascendente ou decrescente. */
    _isSorted(): boolean;
    _isDisabled(): boolean;
}
