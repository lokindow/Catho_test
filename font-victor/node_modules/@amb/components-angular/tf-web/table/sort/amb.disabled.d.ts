import { Constructor } from './amb.constructor';
export interface CanDisable {
    /** Indica se o componente está desabilitado. */
    disabled: boolean;
}
/** Mixin para aumentar uma diretiva com uma propriedade 'disabled'. */
export declare function mixinDisabled<T extends Constructor<{}>>(base: T): Constructor<CanDisable> & T;
