/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { Optional } from '@angular/core';
import { Subject } from 'rxjs/Subject';
/**
 * Para modificar os rótulos e o texto exibido, crie uma nova instância do AMBSortHeaderIntl e
 * incluí-lo em um fornecedor personalizado.
 */
export declare class AMBSortHeaderIntl {
    /**
     * Stream que emite sempre que os rótulos são alterados. Utilizado para notificar
     * se os rótulos tiverem mudado após a inicialização.
     */
    changes: Subject<void>;
    /** Label do botão sorting. */
    sortButtonLabel: (id: string) => string;
    /** Rótulo para descrever o tipo atual (visível apenas para leitores de tela). */
    sortDescriptionLabel: (id: string, direction: "asc" | "desc") => string;
}
export declare function AMB_SORT_HEADER_INTL_PROVIDER_FACTORY(parentIntl: AMBSortHeaderIntl): AMBSortHeaderIntl;
export declare const AMB_SORT_HEADER_INTL_PROVIDER: {
    provide: typeof AMBSortHeaderIntl;
    deps: Optional[][];
    useFactory: (parentIntl: AMBSortHeaderIntl) => AMBSortHeaderIntl;
};
