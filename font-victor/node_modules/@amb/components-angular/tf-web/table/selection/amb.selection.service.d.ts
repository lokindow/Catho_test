import { Observable } from "rxjs/Observable";
import { SelectionModel } from "@angular/cdk/collections";
import { EventSelection } from "./amb.event.selection";
export declare class AMBSelectionService {
    private _selection;
    private _radioSelection;
    private _dataSource;
    private _subject;
    private _observable;
    readonly selection: SelectionModel<any>;
    dataSource: any;
    readonly observable: Observable<EventSelection>;
    readonly radioSelection: SelectionModel<any>;
    isAllSelected(): boolean;
    toggleMasterSelect(): boolean;
    toggleSelect(row: any): boolean;
    radioSelect(row: any): void;
}
