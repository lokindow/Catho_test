/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { ElementRef, ComponentFactoryResolver, Injector, ViewContainerRef, ApplicationRef } from '@angular/core';
import { AMBComponent } from '../../../core/amb.component';
import { AMBTooltipComponent } from '../component/amb.component.tooltip';
export declare class AMBTooltipDirective extends AMBComponent {
    private _elementRef;
    private viewContainerRef;
    private resolver;
    private injector;
    private applicationRef;
    /**
     * Armazena a instância do componente Tooltip
     */
    private component;
    /**
     * Define se o Tooltip será visível.
     */
    private visible;
    /**
     * Define uma mensagem para o Tooltip
     */
    ambTooltip: string | AMBTooltipComponent;
    /**
     * Define se o Tooltip terá animação para ser mostrado.
     */
    tooltipAnimation: boolean;
    /**
     * Define a posição do Tooltip
     */
    tooltipPosition: "top" | "bottom" | "left" | "right";
    /**
     * Construtor padrão;
     * @param _elementRef
     * @param viewContainerRef
     * @param resolver
     * @param injector
     * @param applicationRef
     */
    constructor(_elementRef: ElementRef, viewContainerRef: ViewContainerRef, resolver: ComponentFactoryResolver, injector: Injector, applicationRef: ApplicationRef);
    /**
     * Método responsável por criar e mostrar o Tooltip na tela ao componente que usa a diretivar estiver com foco.
     */
    show(): void;
    /**
     * Método responsável por remover o Tooltip da tela quando o componente que utiliza a diretica perder o foco.
     */
    hide(): void;
}
