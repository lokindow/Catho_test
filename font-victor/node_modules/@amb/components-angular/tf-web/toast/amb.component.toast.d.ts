/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { AMBComponent } from "../../core/amb.component";
import { ElementRef, EventEmitter } from "@angular/core";
import { ToastData } from "./interfaces/amb.toast-data";
/**
 * Definição do componente AMBToastComponent.
 *
 * @selector amb-toast
 * @templateUrl ./amb.component.toast.html
 * @styleUrls ./amb.component.toast.scss
 *
 * @see {@link https://angular.io/api/core/Component}
 */
export declare class AMBToastComponent extends AMBComponent {
    private _elementRef;
    /**
     * Atributo responsável por fornecer as propriedades ao componente toast
     */
    toast: ToastData;
    /**
     * Emissor de evento quando o componente é fechado.
     */
    closeToast: EventEmitter<{}>;
    private timeout;
    /**
     * Construtor padrão reponsável pela criação do componente
     * @param _elementRef - Representa uma instancia do componente.
     */
    constructor(_elementRef: ElementRef);
    /**
      * Método invocado quando o toast é fechado. Realizar uma emissão de evento para o observable.
      */
    close($event: any): void;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    show(): void;
    /**
     * Método responsável por criar os timeouts dos toasts.
     */
    private _setTimeout(toast);
}
