import { ToastData } from "../interfaces/amb.toast-data";
import { ToastConfig } from "../interfaces/amb.toast-config";
import { ToastService } from "../service/amb.service.toast";
/**
 * Definição do componente AMBToastContainer.
 *
 *
 *
 * @selector amb-toast-container
 * @templateUrl ./amb.component.toast-container.html
 *
 * @see {@link https://angular.io/api/core/Component}
 */
export declare class AMBToastContainerComponent {
    private config;
    private toastService;
    /**
     * Constante que indica as possíveis posições.
     */
    static POSITIONS: Array<String>;
    private _position;
    position: string;
    toasts: Array<ToastData>;
    queueToast: Array<ToastData>;
    /**
     * Construtor padrão do container toast
     * @param config - Classe que define as configurações padrões do toast
     * @param toastService - Classe do serviço responsável por criar o toast
     */
    constructor(config: ToastConfig, toastService: ToastService);
    /**
     * Método responsável por observar eventos emitidos pelo serviço e
     * realizar adição ou remoção dos toasts da tela.
     */
    private listenerEventsToast();
    /**
     * Método responsável por observar o evento de fechar do toast e remove-lo.
     */
    closeToast(toast: ToastData): void;
    /**
     * Método responsável por adicionar um novo toast à tela
     */
    add(toast: ToastData): void;
    /**
     * Método responsável por remover o toast através do clique no botão close.
     * @param id identificador único do toast.
     */
    clear(id: number): void;
    /**
     * Método responsável por remover todos os toasts da tela.
     */
    clearAll(): void;
}
