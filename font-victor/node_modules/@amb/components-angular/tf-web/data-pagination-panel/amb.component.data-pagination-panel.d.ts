import { ElementRef, AfterViewInit, OnInit, AfterContentChecked } from '@angular/core';
import { AMBComponent } from '../../core/amb.component';
import { AMBDataFillDirective } from '../data-fill/amb.data-fill.directive';
import { AMBPreLoaderDirective } from '../pre-loader/pre-loader.directive';
import { AMBPreLoaderInterface } from '../pre-loader/amb.pre-loader.interface';
export declare class AMBDataPaginationPanelComponent extends AMBComponent implements OnInit, AfterContentChecked, AfterViewInit, AMBPreLoaderInterface {
    protected elementRef: ElementRef;
    private datafill;
    private preLoader;
    /** Propriedade do componente de execucao de funcao no inicio de slide */
    onSlideStart: Function | null;
    /** Propriedade do componente de execucao de funcao ao termino de slide */
    onSlideEnd: Function | null;
    /** Propriedade do componente de tempo de intervalo entre slides */
    interval: number | null;
    /** Propriedade do componente para permitir retornar ao inicio ou nao */
    wrap: boolean | false;
    /** Propriedade do componente para desabilitar */
    disabled: boolean | false;
    /** Propriedade do componente para desabilitar os botões */
    disabledButtons: boolean | false;
    /** Indica se o componente terá animação automática. */
    animation: boolean;
    /** Contador do slide atual */
    private actual;
    /** Largura do content */
    private contentWidth;
    /** Altura da lista de itens */
    private listHeight;
    /** Número de itens */
    private numberOfItens;
    /** Temporizador timeout */
    private timer;
    /** Maximo de ocorrencias carregadas */
    private maxOcorr;
    /** Estado de dados a direita */
    private stateRight;
    constructor(elementRef: ElementRef, datafill: AMBDataFillDirective);
    /**
       * Método chamado imediatamente depois que as propriedades vinculadas a dados da diretiva foram verificadas para a
       * primeira execução, e antes de qualquer um de seus filhos ter sido verificado. É invocado apenas uma vez quando
       * A diretriz é instanciada.
       *
       * @see {@link https://angular.io/api/core/OnInit}
       */
    ngOnInit(): void;
    /**
       * Método chamado após cada verificação do conteúdo de uma diretiva.
       *
       * @see {@link https://angular.io/api/core/AfterContentChecked}
       */
    ngAfterContentChecked(): void;
    /**
     *   Método chamado após a view de um componente foi inicializada.
     */
    ngAfterViewInit(): void;
    /**
     *   Método chamado para regular os tamanhos dos containers de apresentacao
     */
    private resize();
    /**
     *   Método chamado após o botão da esquerda ser clicado
     */
    prevDataPanel(): void;
    /**
     *   Método chamado após o botão da direita ser clicado.
     */
    nextDataPanel(intervalStatus?: boolean): void;
    /**
     *   Método para carregar os proximos dados pelo adapter e carrega-los na view
     */
    private loadNextData();
    /**
     *   Método para executar o intervalo de tempo para acionar os proximos slides
     */
    private executeInterval(intervalStatus);
    getPreLoader(): AMBPreLoaderDirective;
}
