/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { ElementRef } from '@angular/core';
import { AMBComponent } from '../../../core/amb.component';
import { AMBDuallistComponent } from '../amb.component.duallist';
import { AMBDuallistService } from '../services/amb.duallist.service';
/**
 * Definição do componente AMBDuallistButtonComponent.
 *
 * @selector amb-duallist-button
 * @templateUrl component.duallist-button.html
 * @styleUrls component.duallist-button.scss
 *
 * @see {@link https://angular.io/api/core/Component}
 */
export declare class AMBDuallistButtonComponent extends AMBComponent {
    private duallistService;
    private duallist;
    direction: String;
    /** Indica se a fonte do rótulo do botão será em maiúsculo. */
    uppercase: boolean;
    /** Rótulo do botão. */
    label: string;
    /**
     * Severidade do botão, podendo ser 'info', 'seccess', 'warning' e 'Error'.
     * Se a severidade não for informada, será assumido a severidade padrão.
     */
    severity: string;
    /** Propriedade type define um tipo para o componente Button. */
    type: string;
    /** Propriedade icon define um ícone para o componente Button. */
    icon: string;
    /** Cor da fonte. */
    labelColor: string;
    /** Posição do ícone, podendo ser 'left' ou 'right'. */
    positionIcon: string;
    /**
     * @constructor
     * Construtor padrão.
     *
     * @param elementRef Elemento DOM representando o componente.
     */
    constructor(elementRef: ElementRef, duallistService: AMBDuallistService, duallist: AMBDuallistComponent);
    /**
     * Responsável por obter a classe que representa a severidade configurada.
     * As possíveis são:
     *  - success
     *  - info
     *  - warning
     *  - danger
     *
     * @return string - Classe css da severidade.
     */
    getClassBySeverity(): string;
    /**
     * Verifica se a posição do ícone é à esquerda do rótulo.
     *
     * @return boolean - Indica se a posição do ícone é à esquerda do rótulo.
     */
    isLeftPosition(): boolean;
    /**
     * Obtém o tipo do botão, 'button' ou 'submit'.
     * Para os casos de configuração diferente desses dois tipos, será
     * lançada uma exceção.
     *
     * @return string - Tipo do botão.
     */
    getTypeButton(): string;
    /**
     * Verifica se o botão contém ícone.
     *
     * @return boolean - Se o botão contém ícone.
     */
    hasIcon(): boolean;
    /**
     * Evento para mover item selecionado para a direita
     */
    eventMoveRight(): void;
    /**
     * Evento para mover todos os itens para a direita
     */
    eventMoveAllRight(): void;
    /**
     * Evento para mover item selecionado para a esquerda
     */
    eventMoveLeft(): void;
    /**
     * Evento para mover todos os itens para a esquerda
     */
    eventMoveAllLeft(): void;
    /**
     * Ciclo de vida do Angular
     */
    ngAfterViewInit(): void;
    /**
     * Ciclo de vida do Angular
     */
    ngAfterViewChecked(): void;
}
