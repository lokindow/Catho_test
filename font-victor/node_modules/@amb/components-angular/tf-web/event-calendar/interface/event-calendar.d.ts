import "fullcalendar";
import { FCGeneralOptions } from './event-calendar.general.options';
import { FCContent } from './event-calendar.content';
import { FCEvents } from './event-calendar.events';
export declare class FullCalendar {
    private jqueryElement;
    private _header;
    private _content;
    private _events;
    readonly getGeneralOptions: FCGeneralOptions;
    readonly getContent: FCContent;
    readonly getEvents: FCEvents;
    constructor(jqueryElement: JQuery, options?: any);
    private configCalendar(options?);
    /**
     * Adiciona o icone de evento recorrente aos eventos
     * @param event Evento atual
     * @param element Elemento html
     * @param view View atual do calendario
     */
    eventAfterRenderFn(event: any, element: any, view: any): void;
}
