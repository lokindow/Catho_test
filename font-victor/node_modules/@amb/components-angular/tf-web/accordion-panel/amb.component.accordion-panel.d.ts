/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { ElementRef, EventEmitter } from '@angular/core';
import { AMBComponent } from '../../core/amb.component';
/**
 * Definição do componente AMBAccordionPane.
 *
 * @selector amb-accordion-panel
 * @templateUrl accordion-panel.component.html
 * @styleUrls accordion-panel.component.scss
 *
 * @see {@link https://angular.io/api/core/Component}
 */
export declare class AMBAccordionPanelComponent extends AMBComponent {
    protected elementRef: ElementRef;
    /** Título do componente. */
    accordionTitle: string;
    /**
     * Painel do componente. Este é o container do componente, nele são adicionados
     * os elementos. Este painel é contraido e expandido conforme comportamento do
     * componente.
     */
    /** Indica se o componente está contraido */
    collapsed: boolean;
    /** Função callback a ser chamada quando o componente for expandido. */
    onExpand: Function;
    /** Função callback a ser chamada quando o componente for contraido. */
    onCollapse: Function;
    /** Cor da borda do título. */
    borderColor: string;
    externCollapsed: EventEmitter<{}>;
    /**
     * @constructor
     * Construtor padrão.
     *
     * @param elementRef Elemento DOM representando o componente.
     */
    constructor(elementRef: ElementRef);
    /**
     * Método responsável por contrair ou expandir o componente. Para a animação é
     * utilizado o JQuery, para fazer o slideUp e slideDown.
     *
     * @see {@link http://api.jquery.com/slidetoggle/}
     */
    collapse(): void;
    /**
     * @override
     * Ciclo de vida chamado imediatamente depois que as propriedades vinculadas a dados da diretiva foram verificadas para a
     * primeira execução.
     *
     * @see {@link AMBComponent}
     */
    ngOnInit(): void;
    feedback(): void;
}
