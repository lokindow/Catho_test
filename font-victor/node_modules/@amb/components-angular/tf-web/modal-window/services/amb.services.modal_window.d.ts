import { Injector } from "@angular/core";
import { ModalWindowOptions } from "../interfaces/amb.modal_window-options";
/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
/**
 * @Serviço @name ModalWindowService
 * Serviço para o Tela Modal.
 *
 * @description
 * O serviço ModalWindowService é responsável por criar a Tela Modal
 *
 */
export declare class ModalWindowService {
    private injector;
    /**
     * Atributo responsável por armazenar as opcoes do tela modal
     */
    private options;
    /**
     * Construtor padrão
     * @param injector - responsável por criar o tela modal
     */
    constructor(injector: Injector);
    /**
    * Aciona a criação do tela modal
    */
    createModalWindow(options: ModalWindowOptions): void;
    /**
     * Método responsável por criar o container para o componente
     */
    private show();
    /**
     * Método responsável por preencher as propriedades do componente, de acordo com as informações passadas pelo usuário
     */
    private fillProperties(componente);
}
