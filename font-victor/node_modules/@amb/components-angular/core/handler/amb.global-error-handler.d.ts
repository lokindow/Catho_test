/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
import { ErrorHandler, Injector } from '@angular/core';
/**
 * Classe representando o componente padrão de erros.
 *
 * @description
 * A implementação padrão do ErrorHandler imprime mensagens de erro no console.
 * Para interceptar o tratamento de erros, escreva um manipulador de exceção personalizado
 * que substitui esse padrão conforme apropriado para o aplicativo.
 *
 * @example
 * class CustomErrorHandler implements AMBGlobalErrorHandler {
 *  handleError(error) {
 *   // implementação no caso de erro...
 *  }
 * }
 * @NgModule({
 *  providers: [{provide: ErrorHandler, useClass: CustomErrorHandler}]
 * })
 * class AppModule {}
 *
 * @implements ErrorHandler
 *
 * @see {@link https://angular.io/api/core/ErrorHandler}
 */
export declare class AMBGlobalErrorHandler implements ErrorHandler {
    private injector;
    /**
     * @constructor
     * Construtor padrão.
     */
    constructor(injector: Injector);
    /**
     * A implementação padrão do `GlobalErrorHandler` imprime mensagens de erro no `console`. Para
     * manipulação de erro de interceptação, deve-se escrever um manipulador de exceção personalizado que substitui esse padrão como
     * apropriado para o aplicativo.
     *
     * @param error Objeto de erro.
     *
     * @see AMBComponentFactory
     * @see AMBDomService
     * @see {@link https://angular.io/api/core/ComponentRef}
     */
    handleError(error: any): void;
}
