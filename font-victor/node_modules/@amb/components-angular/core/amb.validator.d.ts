import { Validator, AbstractControl } from "@angular/forms";
import { AMBValidationErrorMessageComponent } from "../tf-web/validation-error-message/amb.component.validation-error-message";
import { ElementRef, AfterViewInit, ComponentFactoryResolver, ViewContainerRef, ApplicationRef, Injector } from "@angular/core";
export declare abstract class AMBValidator implements AfterViewInit, Validator {
    private elementRef;
    private componentFactoryResolver;
    private injector;
    private appRef;
    private viewContainer;
    private _ambValidationErrorMessage;
    readonly ambValidationErrorMessage: AMBValidationErrorMessageComponent;
    private portalHost;
    constructor(elementRef: ElementRef, componentFactoryResolver: ComponentFactoryResolver, injector: Injector, appRef: ApplicationRef, viewContainer: ViewContainerRef);
    ngAfterViewInit(): void;
    private createValidationErrorMessageComponent();
    /**
     * @override
     */
    validate(c: AbstractControl): {
        [key: string]: any;
    };
    protected abstract onBlurValidate(): void;
    protected abstract isValid(any: any): boolean;
}
