import { AjaxRequest } from "rxjs";
import { AMBStorage } from "@amb/core";
import { AMBLoginType } from "../login/amb.login.type";
export declare class AMBCreatorRequest {
    private storage;
    private timeout;
    private contentType;
    private withCredentials;
    private loginService;
    private baseUrl;
    private header;
    constructor(storage: AMBStorage);
    getRequest(path: string): AjaxRequest;
    postRequest(adpterContext: string, data: any): AjaxRequest;
    loginPostRequest(adpterContext: string, data: any, login: AMBLoginType): AjaxRequest;
    headRequest(path: string): AjaxRequest;
    putRequest(adpterContext: string, data: any): AjaxRequest;
    getHeader(): {};
}
