import { AMBException } from '@amb/core';
import { AMBSeveridade } from '@amb/core';
/**
 * @license
 * Copyright Bradesco S/A. All Rights Reserved.
 */
/**
 * @Exceção @name AMBExecutionException
 * Representa o componente padrão de erros para execucoes do JSON Adapter.
 *
 * @description
 * Esta classe define uma exceção base para execucoes do JSON Adapter. Ao ser capturada pelo handler será
 * apresentado um modal mostrando a mensagem informada em sua criação.
 *
 * @see {@link https://www.typescriptlang.org/docs/home.html}
 */
export declare class AMBExecutionException extends AMBException {
    /** Nome da exceção. */
    name: string;
    /** Mensagem de erro. */
    message: string;
    /** Stacktrace do erro. */
    stack?: string;
    /** Código de erro. */
    code?: string;
    /** Tipo do Erro. */
    errType?: string;
    /** Mensagen interna do erro. */
    msgInterna?: string;
    /**
    * @constructor
    * Construtor padrão.
    *
    * @param message Mensagem de erro.
    * @param code Código do erro.
    * @param errType Tipo do Erro.
    * @param msgInterna Mensagem interna do Erro.
    * @param stack Pilha do Erro.
    */
    constructor(message: string, code?: string, errType?: string, msgInterna?: string, stack?: string, severidade?: AMBSeveridade);
}
